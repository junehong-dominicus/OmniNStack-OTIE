apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-processor-service
  labels:
    app: processor
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 50052
      protocol: TCP
      name: grpc
  selector:
    app: processor
