apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-collector-service
  labels:
    app: collector
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: collector
